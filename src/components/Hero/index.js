import styles from "./styles.module.css";

function Hero() {
  return (
    <section className={styles.heroContainer}>
      <div className={styles.heroTitleContainer}></div>
    </section>
  );
}

export default Hero;
